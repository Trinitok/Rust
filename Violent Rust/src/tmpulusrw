use std::fs::File;
use std::io::prelude::*;

///  This is for dictionary attacks against zip files
fn main() -> std::io::Result<()> {
	let mut filee = File::open("test-zip.zip")
	let mut contents = String::new();

	file.read_to_s
}