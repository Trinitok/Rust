use std::env::args;

fn main() {

	let args: Vec<String> = env::args().collect();

	if args.len() < 3{
		println!("usage 'cargo run <target host> <target port'");
	}

	let args = env.args();
	
}